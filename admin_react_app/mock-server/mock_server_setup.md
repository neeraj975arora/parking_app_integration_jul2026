I need to build a separate mock server in node and this code base should be placed outside src folder which is for react application. It should exercise React App through REST Interface. Additionally it should have capability to trace REST APIs logs in both file and console. Optionally it should display content of all payload (unencrypted) if enabled (guarded with flag etc.) So please generate some mock data of 3 months. We would like to test all screens and its functionalities which includes buttons, input,froms, selections etc in the react app.
